This is a minimal Puppet control repo including only `r10k` and `hiera` modules
(plus their dependencies).

For a more complete example, see
[puppet-bootstrap/basic-aio](https://github.com/puppet-bootstrap/basic-aio/).
