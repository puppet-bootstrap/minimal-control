---
version: 5
defaults:
  datadir: /etc/puppetlabs/code/environments/%{::environment}/hieradata
  data_hash: yaml_data
hierarchy:
  - name: "FQDN"
    path: "nodes/%{::fqdn}.yaml"
  - name: "Defaults"
    path: "common.yaml"
